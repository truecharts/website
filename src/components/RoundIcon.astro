---
import { Image } from "astro-preload/components";
const { name, image, profile } = Astro.props;
---

<li class="flex hover:scale-125 duration-300">
  <a href={profile}>
    <Image size="35px"  class="rounded-full !h-8 !w-8 object-cover" alt={name} url={image} />
  </a>
</li>
